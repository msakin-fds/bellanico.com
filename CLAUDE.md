# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Status

This repository is currently uninitialized. It contains only a `README.md` with the project name (`bellanico.com`) and no source code, build configuration, tests, or tooling.

When the project is scaffolded, update this file with:
- Build, lint, test, and dev-server commands (including how to run a single test).
- High-level architecture spanning multiple files (entry points, module boundaries, data flow, external integrations).
- Any conventions established in `.cursor/rules/`, `.cursorrules`, or `.github/copilot-instructions.md` once they exist.
