# Copy this to .env (gitignored) and fill in the values.
# On Claude Code web, .env does not persist across sessions —
# at the start of each new session either paste the app password
# to Claude (Claude will write .env) or recreate this file manually.

SITE_URL=https://bellanico.com
WP_USER=fdsthinkerwp
WP_APP_PASSWORD=          # 24-char WordPress application password
